<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="Waller.Native.App.Controls.ShellHeader"
    x:Name="Root"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Waller.Native.App.Controls">

    <Border
        Padding="20,17"
        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
        BorderThickness="0,0,0,1">
        <Grid ColumnSpacing="16" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="380" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <ComboBox
                Grid.Column="1"
                MinWidth="288"
                AutomationProperties.AutomationId="PresetComboBox"
                AutomationProperties.Name="{Binding ViewModel.Text.Preset, ElementName=Root}"
                DisplayMemberPath="Name"
                IsEnabled="{Binding ViewModel.CanUseShellCommands, ElementName=Root}"
                ItemsSource="{Binding ViewModel.Presets, ElementName=Root}"
                SelectedItem="{Binding ViewModel.SelectedPreset, ElementName=Root, Mode=TwoWay}" />

            <Grid Grid.Column="3" ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <Button
                    Grid.Column="0"
                    MinWidth="112"
                    AutomationProperties.AutomationId="MoreButton"
                    AutomationProperties.Name="{Binding ViewModel.Text.More, ElementName=Root}"
                    IsEnabled="{Binding ViewModel.CanUseShellCommands, ElementName=Root}"
                    ToolTipService.ToolTip="{Binding ViewModel.Text.More, ElementName=Root}">
                    <controls:IconText Glyph="&#xE712;" Text="{Binding ViewModel.Text.More, ElementName=Root}" />
                    <Button.Flyout>
                        <MenuFlyout>
                            <MenuFlyoutItem
                                Text="{x:Bind ViewModel.Text.Save, Mode=OneWay}"
                                Command="{x:Bind ViewModel.SaveCommand, Mode=OneWay}"
                                AutomationProperties.AutomationId="SaveButton"
                                AutomationProperties.Name="{x:Bind ViewModel.Text.Save, Mode=OneWay}">
                                <MenuFlyoutItem.KeyboardAccelerators>
                                    <KeyboardAccelerator Key="S" Modifiers="Control" />
                                </MenuFlyoutItem.KeyboardAccelerators>
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE74E;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutItem
                                Text="{x:Bind ViewModel.Text.SaveAs, Mode=OneWay}"
                                Command="{x:Bind ViewModel.SaveAsCommand, Mode=OneWay}"
                                AutomationProperties.AutomationId="SaveAsButton"
                                AutomationProperties.Name="{x:Bind ViewModel.Text.SaveAs, Mode=OneWay}">
                                <MenuFlyoutItem.KeyboardAccelerators>
                                    <KeyboardAccelerator Key="S" Modifiers="Control,Shift" />
                                </MenuFlyoutItem.KeyboardAccelerators>
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE792;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutItem
                                Text="{x:Bind ViewModel.Text.Manage, Mode=OneWay}"
                                Command="{x:Bind ViewModel.ManagePresetsCommand, Mode=OneWay}"
                                AutomationProperties.AutomationId="ManagePresetsButton"
                                AutomationProperties.Name="{x:Bind ViewModel.Text.Manage, Mode=OneWay}">
                                <MenuFlyoutItem.KeyboardAccelerators>
                                    <KeyboardAccelerator Key="M" Modifiers="Control" />
                                </MenuFlyoutItem.KeyboardAccelerators>
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE713;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutItem
                                Text="{x:Bind ViewModel.Text.Settings, Mode=OneWay}"
                                Command="{x:Bind ViewModel.OpenSettingsCommand, Mode=OneWay}"
                                AutomationProperties.AutomationId="SettingsButton"
                                AutomationProperties.Name="{x:Bind ViewModel.Text.Settings, Mode=OneWay}">
                                <MenuFlyoutItem.KeyboardAccelerators>
                                    <KeyboardAccelerator Key="I" Modifiers="Control" />
                                </MenuFlyoutItem.KeyboardAccelerators>
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE713;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutItem
                                Text="{x:Bind ViewModel.Text.Refresh, Mode=OneWay}"
                                Command="{x:Bind ViewModel.RefreshCommand, Mode=OneWay}"
                                AutomationProperties.AutomationId="RefreshButton"
                                AutomationProperties.Name="{x:Bind ViewModel.Text.Refresh, Mode=OneWay}">
                                <MenuFlyoutItem.KeyboardAccelerators>
                                    <KeyboardAccelerator Key="R" Modifiers="Control" />
                                </MenuFlyoutItem.KeyboardAccelerators>
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE72C;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                        </MenuFlyout>
                    </Button.Flyout>
                </Button>

                <Button
                    Grid.Column="1"
                    MinWidth="176"
                    Background="{ThemeResource WallerPrimaryBrush}"
                    BorderBrush="{ThemeResource WallerPrimaryBrush}"
                    Command="{Binding ViewModel.ApplyAllCommand, ElementName=Root}"
                    AutomationProperties.AutomationId="ApplyAllButton"
                    AutomationProperties.Name="{Binding ViewModel.Text.ApplyAll, ElementName=Root}"
                    Foreground="{ThemeResource WallerPrimaryTextBrush}"
                    IsEnabled="{Binding ViewModel.CanStartApply, ElementName=Root}"
                    ToolTipService.ToolTip="{Binding ViewModel.Text.ApplyAll, ElementName=Root}">
                    <Button.KeyboardAccelerators>
                        <KeyboardAccelerator Key="Enter" Modifiers="Control" />
                    </Button.KeyboardAccelerators>
                    <controls:IconText Glyph="&#xE73E;" Text="{Binding ViewModel.Text.ApplyAll, ElementName=Root}" />
                </Button>
            </Grid>
        </Grid>
    </Border>
</UserControl>
